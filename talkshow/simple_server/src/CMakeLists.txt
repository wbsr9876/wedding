AUX_SOURCE_DIRECTORY(./ SRC)

INCLUDE_DIRECTORIES("../../RakNet/include/raknet")

LINK_DIRECTORIES("../../RakNet/Lib/DLL")

ADD_EXECUTABLE(server ${SRC})

TARGET_LINK_LIBRARIES(server pthread RakNetDLL)
